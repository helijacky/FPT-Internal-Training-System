@model test9.Models.CourseCategories

@{
    ViewBag.Title = "Delete";
}
@if (User.IsInRole("Trainer"))
{
    <h2>Delete</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>

        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.CourseCatName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.CourseCatName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.CourseCatDes)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.CourseCatDes)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
}